import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Poppins } from 'next/font/google';

const poppins = Poppins({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700', '800'],
  variable: '--font-poppins',
});

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SENCO PAYMENT TRACKER",
  description: "SENCO Payment Tracker is a dedicated financial monitoring tool designed to streamline the collection of class funds for Senior Class Officers. By integrating real-time data from secure cloud sources, the app provides students with instant transparency regarding their payment status, remaining balances, and full transaction history.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      {/* Apply the font class to the body */}
      <body className={poppins.className}>{children}</body>
    </html>
  );
}
